# Современник — v24 audit fixes

Эта версия собрана после проверки проекта.

Что исправлено:
- очищены повторяющиеся override-функции в `assets/js/app.js`;
- `init()` перенесен в конец файла, чтобы приложение запускалось после объявления финальных функций;
- сохранен рабочий `VAPID_PUBLIC_KEY` из текущего GitHub-файла `assets/js/supabase-config.js`;
- добавлен SQL-патч `supabase/sql/STEP_7_FIX_RLS_CONTENT_SYNC.sql` для исправления RLS-доступа к фото и редактированию тех. карт;
- удалены лишние служебные `.gitkeep` из непустых папок с изображениями;
- проверены ссылки на CSS/JS/manifest/icon/menu images/documents;
- проверены основные активные кнопки по HTML `data-*` атрибутам и обработчикам событий;
- проверена валидность JavaScript и JSON.

Перед загрузкой:
1. Загрузите содержимое архива в корень GitHub Pages репозитория.
2. Если ранее была ошибка RLS при сохранении фото, выполните SQL-файл `supabase/sql/STEP_7_FIX_RLS_CONTENT_SYNC.sql` в Supabase SQL Editor.
3. После загрузки сайта выполните жесткое обновление браузера: Ctrl+Shift+R.
