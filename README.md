# Современник · сервис сотрудников

В этой версии добавлены:

- все чек-листы свернуты по умолчанию и открываются по нажатию;
- отправка доступна для всех чек-листов;
- после отправки показывается окно: «Отлично! Чек-Лист отправлен»;
- пользователь остается в текущей вкладке, перехода в «Контроль» нет;
- добавлен раздел «Ревизии» с формой ежедневной ревизии кофе;
- в «Контроль» добавлены две папки: «Чек-листы» и «Ревизии»;
- данные чек-листов и ревизии кофе отправляются в Google Sheets через Apps Script.

## Важно: обновите Apps Script

Чтобы ревизия кофе записывалась в лист `Ревизия Кофе`, нужно заменить код в Google Apps Script на код из файла:

```text
google_apps_script_code.js
```

После замены кода нужно заново нажать:

```text
Deploy → Manage deployments → Edit → New version → Deploy
```

Ссылка Web App может остаться прежней. Она уже подключена в `assets/js/app.js`.

## Загрузка на GitHub Pages

Распакуйте архив и загрузите содержимое папки в корень репозитория. В корне должны лежать:

```text
index.html
assets/
data/
README.md
.nojekyll
google_apps_script_code.js
```

Если GitHub Pages уже включен, дополнительных настроек обычно не нужно.

## Обновление v6: синхронизация ревизий

В этой версии ревизия кофе синхронизируется по дате ревизии. Сотрудник и руководитель могут заполнять данные отдельно, но если выбрана одна дата, данные попадают в одну колонку.

После загрузки сайта обязательно обновите Google Apps Script файлом `google_apps_script_code.js` и создайте новую версию деплоя.


## Система ролей и входа

В этой версии сайт открывается со страницы входа. Стартовый аккаунт администратора создается автоматически в Google Apps Script:

- логин: `grigory`
- пароль: `0808`
- роль: `Администратор`
- имя: `Григорий`

После замены кода Apps Script и нового деплоя появится лист `Сотрудники` с аккаунтами. Раздел `Сотрудники` доступен только администратору, потому что в нем отображаются логины и пароли.

После загрузки файлов в GitHub обязательно замените код Apps Script на файл `google_apps_script_code.js` из архива и сделайте новый деплой: `Deploy → Manage deployments → Edit → New version → Deploy`.


## Обновление v10 — задачи, ошибки, расписание

В этой версии добавлены:
- главный экран с таск-трекером;
- создание задач сотрудниками друг другу;
- раздел «Сообщить об ошибке»;
- папка «Ошибки» во вкладке «Контроль»;
- раздел «Расписание» с переходом по месяцам и ручным добавлением мероприятий.

После загрузки сайта на GitHub Pages обязательно замените код Google Apps Script на `google_apps_script_code.js` из этого архива и сделайте новый деплой.
